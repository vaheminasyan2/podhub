import React from "react";

// BUTTON COMPONENT

function Button ({ type, text }) {

    return (

        <div>
            <button>{text}</button>
        </div>
    )
}

export default Button;